"""
This is the view layer `M` in the MVC design. Access to
database and all of the operations should be done through
using models. Note that in flask we use models instead of
DAO.
"""
from .user import User
